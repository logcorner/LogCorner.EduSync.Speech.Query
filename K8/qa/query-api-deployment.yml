apiVersion: apps/v1
kind: Deployment

metadata:
  name: speech-query-api-deployment
  namespace: qa
  labels:
    version: 1.0.0
    product: speech-http-query-api
    environment: qa

spec:
  replicas: 1
  selector:
    matchLabels:
      app: speech-http-query-api
      env: qa
  template:
    metadata:
      labels:
        app: speech-http-query-api
        env: qa
    spec:
      containers:
      - name: speech-http-query-api
        image: logcornerhub/logcorner-edusync-speech-query
        imagePullPolicy: Never
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Kubernetes.qa"
        ports:
        - containerPort: 80
        - containerPort: 443
          protocol: TCP
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
    